import org.jenkinsci.test.acceptance.controller.Ec2Provider
import org.jenkinsci.test.acceptance.controller.JenkinsController
import org.jenkinsci.test.acceptance.controller.JenkinsProvider
import org.jenkinsci.test.acceptance.controller.Machine

key = "..."
secret = "..."

//XXX: How to bind it to the scope of a test suite? It gets injected per test - we want it to be injected
//only for set of tests. Maybe MachineProvider.get() can implement such rules to return perviously created
//Machine instances but what those rules would be? Something like @TestSuiteScope?
bind Machine toProvider Ec2Provider

bind JenkinsController toProvider JenkinsProvider

