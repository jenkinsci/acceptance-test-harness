// test launch configuration that runs with an existing instance
// bind(JenkinsController).toInstance(new ScaffoldController())

bind(JenkinsController).toInstance(new org.jenkinsci.test.acceptance.server.PooledJenkinsController())
