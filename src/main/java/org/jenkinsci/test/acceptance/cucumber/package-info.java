/**
 * Glue to write acceptance tests in Cucumber.
 *
 * The way Cucumber is written requires that we put some of the glue into the "cucumber.runtime" package,
 * so some code do live there.
 */
package org.jenkinsci.test.acceptance.cucumber;